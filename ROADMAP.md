# cnb roadmap

最后更新：2026-05-08 Shift 001

8 个 open issues，按依赖关系分三个阶段。后面的阶段依赖前面的，但同阶段内可以并行。

---

## Phase 1 — 基础稳固

先把地基打好。不做这些，后面的功能都是空中楼阁。

### ~~`.claudes/` → `.cnb/` 重命名 (#39)~~ ✅ 已完成

向后兼容方式实现：新项目用 `.cnb/`，旧项目自动 fallback `.claudes/`，无需迁移。

### 待办操作队列 (#34)

| | |
|---|---|
| **为什么先做** | 飞书授权、npm 登录等需要用户介入的操作没有闭环机制 |
| **现状** | forge 基础框架已完成，需要 verify/retry 流程打通 |
| **负责人** | forge |
| **依赖** | 无 |

### 自动更新检查 (#43)

| | |
|---|---|
| **为什么先做** | 同学和用户可能跑着旧版 cnb 而不知道 |
| **做什么** | 检测版本过期 → 通知 cnb 负责同学更新（虚拟环境跳过）。是否需要用户确认待定 |
| **现状** | `bin/cnb` 启动时已有基础检查，需扩展到同学子命令触发、负责同学执行 |
| **负责人** | 待定 |
| **依赖** | 无 |

### Token 用量追踪 (#38)

| | |
|---|---|
| **为什么先做** | 不知道花多少钱就是盲人摸象。团队管理的前提是可观测 |
| **方案** | 解析 `~/.claude/projects/` 下的 JSONL，聚合 per-session token 数 + 估算费用 |
| **产出** | `cnb usage` 命令 |
| **负责人** | 待定 |
| **依赖** | 无 |

---

## Phase 2 — 完整的 ownership

有了稳固基础，让负责人真正能独立完成工作——不需要人盯着才能推进。

### ~~负责人自治能力~~ ✅ 核心已完成 (#45)

已实现:
- ✅ **Ownership 注册**: `board own claim/list/disown/map`，路径级 ownership
- ✅ **完成验证**: `task done` 自动跑 pytest，不过不标 done（`--skip-verify` 可跳过）
- ✅ **产出交付**: `task done` 成功后自动 `gh pr create`（feature branch）
- ✅ **任务感知**: `board scan` 扫描 GitHub issues + CI 状态，按 ownership 路由通知

待优化（后续 issue）:
- 外部触发自动化（webhook/polling 替代手动 `board scan`）
- 基于 git blame 的 ownership 自动推荐
- ownership 冲突解决机制

### 自动化下班流程 (#41)

| | |
|---|---|
| **做什么** | `cnb shutdown`：广播收工 → 等 ack → 收集日报 → 生成轮次汇总 → 关停 |
| **为什么重要** | 当前下班全靠手动，外部会话 sleep 60 就硬杀。数据丢失风险 |
| **包含** | graceful shutdown（原 #40 已合并进来） |
| **负责人** | 待定 |
| **依赖** | #39（路径要定下来再写持久化） |

### 全局 dashboard + 巡检 (#42)

| | |
|---|---|
| **做什么** | `cnb global status/sweep/shutdown`，全机器项目管理 |
| **包含** | 孤儿 tmux 清理、stale lock 释放、WAL checkpoint、僵尸 dispatcher 清理 |
| **负责人** | forge（musk 协助安全部分） |
| **依赖** | #36（已完成）、#39（路径）、#41（shutdown 协议） |

### 自适应团队 (#30)

| | |
|---|---|
| **做什么** | 按需扩缩同学数量 + 保护有 ownership 的同学不被随意 kill |
| **三步走** | 1) 采集指标 → 2) ownership 保护 → 3) 自动扩缩（数据驱动，不搞手工规则） |
| **负责人** | 待定 |
| **依赖** | #38（需要指标数据）、#41（需要 graceful shutdown） |

---

## Phase 3 — 远景

方向对但还不急。需要 Phase 1-2 的基础设施成熟后再动手。

### 架构债务清理 (#26)

| | |
|---|---|
| **核心问题** | SQLite 不支持跨机器、双重状态源（DB + .md 文件）、pgrep 服务发现太脆弱 |
| **前置决策** | 跨机器传输层选型（sync 协议？relay？CRDTs？）。不决定这个，其他都是补丁 |
| **负责人** | 待定（需要架构讨论） |
| **依赖** | Phase 2 全部完成后再碰，否则改了底层上层全崩 |

### 云端 / 托管运行

| | |
|---|---|
| **做什么** | 让用户无需本地环境即可使用 cnb |
| **可能的路径** | 1) `.devcontainer/` 支持 GitHub Codespaces（最轻量）2) Web dashboard + 远程 tmux 3) 完整托管服务（容器替代 tmux，Web UI 替代终端） |
| **前置条件** | Phase 1-2 全部完成，项目本身稳定 |
| **负责人** | 随缘，社区贡献欢迎 |
| **依赖** | #26（架构需要支持远程运行） |

### 知识共享系统 (#29)

| | |
|---|---|
| **核心 idea** | 知识是代码不是文本，检索靠执行不靠搜索 |
| **现状** | musk 和 lisa-su 讨论收敛到"可执行断言三元组"方向，还在设计阶段 |
| **负责人** | 待定 |
| **依赖** | #26（需要跨机器基础设施才能做知识传播） |

---

## 依赖图

```
#39 重命名 ─────┬──→ #41 自动下班 ──→ #42 全局管理
                │                  ──→ #30 自适应团队
#34 待办队列    │
#38 token 追踪 ─┘──→ #30 自适应团队
#43 自动更新

                     Phase 1-2 全部 ──→ #26 架构债务
                                   ──→ #29 知识共享
```

## 负责人分配现状

| 同学 | 已负责 | 建议追加 |
|------|--------|----------|
| forge | #34 待办队列 | #42 全局管理 |
| musk | #31 隔离（已关） | #39 重命名（熟悉路径体系） |
| lisa-su | #33 通知（已关） | #41 自动下班（熟悉 dispatcher/concern） |
| tester | 测试加固 | #38 token 追踪（需要解析 JSONL，测试覆盖） |

#26 和 #29 暂不分配，等 Phase 2 收尾后架构讨论再定。
