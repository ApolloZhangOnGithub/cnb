---
allowed-tools: Bash(board *), Bash(board --*), Bash(board - *)
argument-hint: "<subcommand + args>"
description: board CLI pass-through — board $ARGUMENTS
---

!`$CLAUDE_PROJECT_DIR/bin/board $ARGUMENTS`

Reply with only the command output above.
