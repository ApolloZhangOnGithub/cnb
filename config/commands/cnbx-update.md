---
allowed-tools: Bash(pip install --upgrade claude-nb), Bash(pip3 install --upgrade claude-nb)
description: update CLI — pip install --upgrade claude-nb
---

!`pip install --upgrade claude-nb`

Reply with only the command output above.
